{
  "name": "optics-lab-frontend",
  "version": "1.0.0",
  "description": "Frontend for Interactive Optics Lab",
  "main": "index.html",
  "scripts": {
    "dev": "http-server . -p 8080 -c-1 --cors",
    "backend": "cd backend && npm run dev",
    "backend-install": "cd backend && npm install",
    "full-dev": "concurrently \"cd backend && npm run dev\" \"http-server . -p 8080 -c-1 --cors\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "optics",
    "simulation",
    "education",
    "frontend"
  ],
  "author": "OpticsLab Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "http-server": "^14.1.1"
  }
}
